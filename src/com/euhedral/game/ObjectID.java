package com.euhedral.game;

// An enumeration of all possible Object IDs. Player is available by default because most games have a player object
public enum ObjectID {
    Player,
    // Add other objectIDS
}
